React + TypeScript 函数组件；UI 基于 Tailwind CSS 与 `lucide-react` 图标；国际化使用 `react-i18next`；数据获取统一通过 `@/lib/api` 暴露的 `listRuns`/`createScheduledRun`/`listScheduledRuns`/`deleteScheduledRun`/`getLiveStatus`/`verifyConnector` 等方法，并使用 `AbortController` 实现请求取消。