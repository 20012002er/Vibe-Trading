本地预览使用 `cd wiki && python3 -m http.server 8088` 启动简单 HTTP 服务（不应用 `_redirects` 重写规则）；生产/预览部署使用 `cd wiki && wrangler pages dev` 或 `pages deploy`，需在 Cloudflare 控制台配置 Pages 项目 root 为 `wiki`、输出目录为 `.`、自定义域 `vibetrading.wiki`。