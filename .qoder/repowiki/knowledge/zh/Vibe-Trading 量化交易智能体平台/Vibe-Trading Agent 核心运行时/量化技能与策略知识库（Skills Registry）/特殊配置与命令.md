新增技能需在子目录创建 `SKILL.md` 并在 YAML front matter 中填写 `name`、`description`、`category`；如需可执行信号引擎，需实现 `SignalEngine.generate` 契约并将示例放入 `example_signal_engine.py`；数据源类技能需在 `references/` 下按主题组织接口文档，并在 `scripts/` 提供最小可运行调用示例。