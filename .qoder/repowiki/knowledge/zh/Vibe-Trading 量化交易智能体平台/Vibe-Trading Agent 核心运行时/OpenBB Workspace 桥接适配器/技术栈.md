FastAPI + sse_starlette 的 EventSourceResponse 实现 SSE 流；依赖可选的 `openbb-ai` SDK（`openbb_ai.models.QueryRequest`、`openbb_ai.helpers.message_chunk/reasoning_step`），通过 try/except 在运行时检测是否安装；Pydantic v2 模型用于清单序列化。