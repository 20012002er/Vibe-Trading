运行时状态全部位于 `~/.vibe-trading/live/<broker>/` 下：`runtime_state.json` 保存对账基线，`HALT` 哨兵文件用于 kill switch，`trade_counter.json` 记录每日下单计数；`trip_halt` / `clear_halt` 通过同目录 temp + `os.replace` 原子写入哨兵，权限设为 0o700。