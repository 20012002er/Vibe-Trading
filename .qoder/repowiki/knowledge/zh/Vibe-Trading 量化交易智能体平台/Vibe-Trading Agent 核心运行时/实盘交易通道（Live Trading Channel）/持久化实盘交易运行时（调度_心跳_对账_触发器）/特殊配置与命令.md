无独立构建命令；运行时状态落盘在 `live_root()/runtime/jobs.json`（job store）与 `live_root()/runtime/heartbeats/<runner_id>.heartbeat`（心跳），以及各 broker 目录下的 `runtime_state.json`（对账基线）。首次启动若存在损坏的 jobs.json 会抛出 `CorruptJobStoreError` 并 quarantine 该文件，需人工修复。