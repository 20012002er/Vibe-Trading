`ChannelRuntime.start(start_manager=True)` 会同时拉起 `ChannelManager.start_all()` 与各平台适配器；会话映射默认落盘至 `<data_dir>/channels/sessions.json`，配对码默认 TTL 600 秒；`/pairing list|approve <code>|deny <code>|revoke [channel] user_id` 为内置管理命令。