各适配器需额外安装对应 SDK（如 `pip install python-telegram-bot slack_sdk discord.py websockets`），并通过配置文件中的 `enabled` 字段与凭据（bot_token/app_token/token 等）启用；WebSocket 通道支持 Unix socket、TLS（ssl_certfile/keyfile）及短令牌签发路径 `token_issue_path`。