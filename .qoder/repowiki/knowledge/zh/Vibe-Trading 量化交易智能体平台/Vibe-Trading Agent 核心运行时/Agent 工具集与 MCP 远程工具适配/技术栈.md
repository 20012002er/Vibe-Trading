Python；工具基类来自 `src.agent.tools.BaseTool`；远程工具通过 `fastmcp` 客户端（stdio/SSE/Streamable HTTP 传输）+ `mcp.types` 协议；OAuth 令牌使用 `key_value.aio.stores.filetree.FileTreeStore` 持久化到 0700 权限目录；OCR 支持 LLM vision 与 rapid-ocr 两种后端。