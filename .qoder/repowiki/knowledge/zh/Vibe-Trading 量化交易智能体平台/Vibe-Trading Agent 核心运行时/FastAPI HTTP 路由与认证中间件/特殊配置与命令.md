路由模块不会自行创建 FastAPI app，必须由宿主 `agent/api_server.py` 调用各自的 `register_*_routes(app, ...)` 完成挂载；若未在 `sys.modules` 中找到 `api_server`，注册函数会抛出 `RuntimeError` 要求显式传入依赖。SSE ticket 默认 TTL 60 秒且单次消费后失效，浏览器需先 POST `/auth/sse-ticket` 再携带 `?ticket=` 打开 EventSource。