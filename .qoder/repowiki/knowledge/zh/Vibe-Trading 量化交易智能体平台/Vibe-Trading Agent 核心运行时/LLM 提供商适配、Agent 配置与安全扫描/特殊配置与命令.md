.env 文件按固定优先级自动加载：`~/.vibe-trading/.env` → `<AGENT_DIR>/.env` → `<CWD>/.env`，首次加载后通过 `reset_env_config()` 刷新缓存；可通过 `VIBE_TRADING_DISABLE_HTTP_PROXY` 强制禁用系统代理环境变量；swarm 模式通过 `VIBE_TRADING_SWARM_AGENT_CONFIG` 覆盖运行期配置路径。