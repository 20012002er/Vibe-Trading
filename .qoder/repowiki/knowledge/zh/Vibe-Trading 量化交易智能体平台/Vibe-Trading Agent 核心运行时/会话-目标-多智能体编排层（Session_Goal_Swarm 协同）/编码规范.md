- 各子模块均通过独立的 store.py 封装 SQLite 访问，并由上层 service/runtime 按需懒加载实例以避免启动开销
- 对外暴露的事件与状态变更统一通过 src.session.events.SSEEvent 发布，供 websocket/channels 订阅转发
- Agent 侧对 goal 与 swarm 的调用采用延迟 import（在函数体内 from src.goal / from src.swarm），避免冷启动依赖
- Swarm 工作流以 presets/*.yaml 文件声明式定义，运行时由 presets.py 动态加载而非硬编码