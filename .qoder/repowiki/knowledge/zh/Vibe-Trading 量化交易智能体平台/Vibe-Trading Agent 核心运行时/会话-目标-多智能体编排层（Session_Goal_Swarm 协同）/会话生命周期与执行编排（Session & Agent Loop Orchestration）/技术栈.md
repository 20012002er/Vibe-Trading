Python dataclass + enum 建模；SQLite FTS5（WAL 模式）作为跨会话全文检索索引；SSE 事件总线基于 asyncio.Queue + threading.Lock；文件系统 JSON/JSONL 持久化；通过 `concurrent.futures.ThreadPoolExecutor(max_workers=4)` 隔离阻塞式工具注册与 AgentLoop 执行。