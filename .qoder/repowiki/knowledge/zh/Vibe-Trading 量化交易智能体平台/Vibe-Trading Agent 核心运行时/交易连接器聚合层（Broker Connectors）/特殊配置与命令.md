Alpaca 可选启用 TAP 代理：设置环境变量 `TAP_PROXY_URL` + `TAP_AGENT_KEY`（以及可选 `TAP_ALPACA_CREDENTIAL`）后，所有读/写请求经 TAP 转发并由服务端注入 `<CREDENTIAL:...>` 占位符密钥，agent 进程不再持有 Alpaca 密钥；IBKR 本地模式需运行中的 TWS/IB Gateway 监听默认端口（paper 7497/4002，live 7496/4001），且配置保存在 `~/.vibe-trading/ibkr-local.json`。