`vibe-trading alpha list|show|bench|compare|export-manifest` 与 `vibe-trading hypothesis list|show|invalidate` 是主要入口；`alpha bench` 默认走 `bench_runner`，加 `--strict` 切换至 `bench_runner_strict` 并支持 `--oos-split` 与 `--random-seeds`；`alpha export-manifest --out PATH` 默认拒绝写入仓库根之外路径，需 `--force` 放行。