入口命令为 `python -m backtest.runner <run_dir>`；策略文件需满足 `SignalEngine.__init__()` 无必需参数且提供 `generate(data_map) -> Dict[str, pd.Series]`；可通过环境变量 `VIBE_TRADING_DATA_CACHE`/`VIBE_TRADING_DATA_CACHE_ROOT` 启用本地 loader 缓存，`RSSHUB_BASE_URL` 控制事件 feed。