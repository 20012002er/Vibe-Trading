通过环境变量启用/配置行为：`VIBE_TRADING_DATA_CACHE` 开启本地 parquet 缓存，`VIBE_TRADING_DATA_CACHE_ROOT` 指定缓存根目录；各 provider 使用独立最小间隔变量（如 `VIBE_TRADING_EASTMONEY_MIN_INTERVAL`）；`CCXT_TIMEOUT_MS`、`CCXT_FETCH_BUDGET_S` 控制 CCXT 超时与预算；`TUSHARE_TOKEN` 等凭据来自 `src.config.accessor.get_env_config()`。