通过 `python -m backtest.runner <run_dir>` 启动，读取 run_dir 下的 config.json（codes/start_date/end_date/source/interval/engine/initial_cash/fundamental_fields/event_feeds），支持 source="auto" 按符号自动路由到 tushare/yfinance/okx/ccxt/akshare 等数据源。