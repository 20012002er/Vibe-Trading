运行回测统一调用各引擎的 `run_backtest(config, loader, signal_engine, run_dir)`；启用严格永续模式需满足 `perpetual_strict=True` 且 `interval` 为 1m/5m/15m/30m/1h，否则抛错；混合货币代码集在 `CompositeEngine.run_backtest` 中会被 `_reject_mixed_currency` 拒绝。