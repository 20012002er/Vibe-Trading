`pip install -e .` 安装后可通过 `vibe-trading` REPL 或 `vibe-trading serve --host 0.0.0.0 --port 8899` 启动后端；`docker compose up` 一键拉起后端与可选前端开发服务；`python -m pytest -m unit|integration` 在 agent/tests 下运行分层测试；CI 通过 ci_scripts 执行 AST 与 grep 安全门禁。