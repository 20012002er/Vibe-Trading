入口为 `python -m cli` 或安装后的 `vibe-trading` 控制台脚本；首次启动若未检测到任何 provider `.env`（~/.vibe-trading/.env、项目 .env、CWD .env）会进入 onboarding 向导；性能基准脚本位于 `agent/scripts/bench_performance.py`，需从仓库根目录以 `python agent/scripts/bench_performance.py` 运行。